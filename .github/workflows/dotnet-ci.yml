name: .NET 8 CI Pipeline with Docker

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: 📥 Repo Checkout
      uses: actions/checkout@v4

    - name: ⚙️ Setup .NET 8
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: '8.0.x'

    - name: 📦 Restore Dependencies
      run: dotnet restore

    - name: 🏗️ Build Project
      run: dotnet build --no-restore

    - name: ✅ Run Tests
      run: dotnet test --no-build --verbosity normal

    - name: 🐳 Build Docker Image
      working-directory: DotnetDevopsPipeline.Api
      run: docker build -t dotnetdevopspipeline .

